services:
  - type: web
    name: jp-downloader
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: MAX_CONCURRENT
        value: "1"
      - key: MAX_QUEUE
        value: "10"
      - key: RATE_MAX
        value: "5"
